import { RequestHandler } from "express";
import { ProxyOAuthServerProvider } from "./providers/proxy-provider";
import { OAuthProxyConfig } from "./types";
export interface OAuthProxy {
    provider: ProxyOAuthServerProvider;
    router: RequestHandler;
    middleware: RequestHandler;
}
/**
 * Creates an OAuth proxy setup
 */
export declare function createOAuthProxy(config: OAuthProxyConfig): OAuthProxy;
