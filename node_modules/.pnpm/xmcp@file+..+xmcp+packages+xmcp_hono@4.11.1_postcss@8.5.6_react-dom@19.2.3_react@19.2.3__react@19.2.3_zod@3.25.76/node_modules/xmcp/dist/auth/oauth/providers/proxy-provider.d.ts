import { ProxyOAuthServerProvider as IProxyOAuthServerProvider, ProxyOAuthProviderConfig, AccessToken, AuthorizeParams, TokenParams, RevokeParams, TokenResponse } from "../types";
export declare class ProxyOAuthServerProvider implements IProxyOAuthServerProvider {
    private config;
    private storage;
    constructor(config: ProxyOAuthProviderConfig);
    get endpoints(): import("../types").OAuthEndpoints;
    verifyAccessToken(token: string): Promise<AccessToken>;
    authorize(params: AuthorizeParams): Promise<string>;
    token(params: TokenParams): Promise<TokenResponse>;
    revoke(params: RevokeParams): Promise<void>;
    private verifyTokenWithProvider;
    private createOAuthError;
}
